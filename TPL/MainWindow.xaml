<Window x:Class="TPL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="1" Margin="10" x:Name="lbColumn"></ListBox>
        <StackPanel>
            <Button Margin="35" Height="45" Content="Start" x:Name="btnStart" FontSize="25" Padding="0" Click="btnStart_Click"/>
            <TextBox Margin="10" Height="105" x:Name="tbIcon"/>
        </StackPanel>
    </Grid>
</Window>
